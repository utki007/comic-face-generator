# Dependencies for Face2Comic inference API (FastAPI) + React frontend dev workflow
# (training/eval notebooks may require additional packages)

pillow
numpy
torch
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
